#include "main.h"
#include <stdio.h>

/**
 * _strlen - Determines the length of a string
 * @s: param
 * Return: returns the length of a string
 */
int main()
{
	char *str;

	str = "My school";
	printf("%c\n", str[1]);
	
	return (0);
}
